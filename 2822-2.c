#include <stdio.h>
#include <malloc.h>

const int MAX = 8;

int main(void) {
    int score[MAX];
    int index[MAX];

    for (int i = 0; i < MAX; i++) {
        scanf("%d", score[i]);
    }

    printf("%d");

    for (int i = 0; i < MAX; i++) {

    }
    return 0;
}
